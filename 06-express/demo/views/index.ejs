<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>订餐系统</title>
    <style>
        .box{
            width: 400px;
            height: 400px;
            border: 1px solid red;
            margin: 50px auto;
        }
        .box{
            text-align: center;
        }
    </style>
</head>
<body>
    <div class="box">
        <h1>订餐系统</h1>
        <!-- form action="/save" -->
        <p>请输入手机号：<input type="text" name="shoujihao"></p>
        <p>你想要吃什么：<input type="text" name="cai"></p>
        <!-- 把输入框中的内容扔给服务器：1）form action   2）ajax 也可以 -->
        <p><input type="button" value="确认" id="btn"></p>
    </div>
</body>
<!-- 引入jq的路径需要改变一下  暂时可以先不理解 -->
<script src="/jquery1.11.3.js"></script>
<script>

    $("#btn").click(function(){
        console.log(".....")
        // 得到输入框中的数据 
        let shoujihao = $("input[name=shoujihao]").val();
        let cai = $("input[name=cai]").val();

        // 浏览器地址栏   src   href   postman   ajax 
        // ajax去请求服务器是没有跨域问题？  因为此时前面和后面处于同一个域于
        // 如果前端页面是通过服务端渲染的，是没有跨域问题
        $.post("/save",{
            shoujihao:shoujihao, // 第1个shoujihao表示名字  第2个shoujihao表示真实的数据
            cai:cai
        },function(data){  // data就是服务器返回的数据  
            // data可以告诉你请求是否成功 
            if(data == 1){  // 前后端协商好的标识符
                alert("成功")
            }else{
                alert("失败")
            }
        })
    })

</script>
</html> 